---
const { title, description, image, link } = Astro.props;
---

<div class="project-card">
  <a href={link}>
    <img src={image} alt={title} />
    <h3>{title}</h3>
    <p>{description}</p>
  </a>
</div>